<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Website</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="icon" href="/img/favicon.ico">
</head>

<body>
    <nav class="container mx-auto flex items-center justify-between py-4 px-16">
        <div>
            <a href="/"><img src="/img/logo.png" alt="logo"></a>
        </div>
        <div>
            <ul class="flex items-center">
                <li class="ml-6"><a href="#">Menu</a></li>
                <li class="ml-6"><a href="#">Offers</a></li>
                <% if(user) { %>
                    <li class="ml-6">
                        <form id="logout" action="/logout" method="post">
                            <a href="#" onclick="document.getElementById('logout').submit()">Logout</a>
                        </form>
                    </li>
                    <% } else { %>
                        <li class="ml-6"><a href="/register">Register</a></li>
                        <li class="ml-6"><a href="/login">Login</a></li>
                        <% } %>
                            <li class="ml-6"><a class="inline-block px-4 py-2 rounded-full flex items-center"
                                    href="/cart">
                                    <span class="text-white font-bold pr-2" id="cartCounter">
                                        <%= session.cart ? session.cart.totalQty : '' %>
                                    </span>
                                    <img src="/img/cart.png" alt=""></a></li>
            </ul>
        </div>
    </nav>
    <%- body %>
        <footer></footer>
        <script src="/js/app.js"></script>
</body>

</html>